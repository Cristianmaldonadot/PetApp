<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="20dp" /> <!-- ajusta el radio según el grado de redondeo deseado -->
    <solid android:color="@android:color/white" />
    <stroke
        android:width="2dp"
        android:color="@color/bordeInput" />
    <!-- puedes ajustar el color del fondo aquí -->
</shape>